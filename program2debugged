#include <stdio.h>
int avg(a,b)
{int avg;
avg=a+b/2;
return avg;
}
intavg(c,d,e)
{int avg;
avg=c+d+e/2;
return avg;
}
int main()
{
    int ans;
    
    ans = avg(4,5);
    printf("Average of the numbers is: %d",ans);

    
}
